"""dadtool — import audio into Dead as Disco with offline-analyzed beat sync.

The game is treated as an external, configurable target. Nothing outside
``paths.py`` may hardcode the game directory.
"""

__version__ = "0.1.0"
